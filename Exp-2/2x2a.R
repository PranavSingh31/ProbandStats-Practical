n<-as.integer(readline(prompt="Enter a number: ")) #taking input
print(pbirthday(n, classes = 365, coincident = 2))

#using pbirthday function that measures the coincident factor
#n stands for number of sample
#classes is today spaces
#coincident = 2 number of people matching the condition
